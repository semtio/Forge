# 🔧 Исправление: Адаптивные изображения (srcset)

## 📋 Проблема
Изображения на сайте не выводились с атрибутом `srcset`, что означало:
- ❌ Браузер не мог выбрать оптимальный размер изображения
- ❌ На мобилках загружались большие файлы вместо оптимизированных
- ❌ Медленная загрузка сайта на медленных соединениях

## 🔍 Причина
Изображения в TinyMCE редактируемом контенте (`plugins/tinymce/data/*.html`) **загружаются динамически** через функцию `loadTinyContent()`.

Эти изображения **не проходили** через `auto-alt.php` (который использует output buffering), потому что:
1. `auto-alt.php` регистрирует `ob_start()` callback в `header.php`
2. Контент загружается через `loadTinyContent()` уже **внутри** этого буфера
3. Output buffering на момент загрузки уже был создан, поэтому новый контент не обрабатывается

## ✅ Решение

### Изменено 3 файла:

#### 1. **`plugins/tinymce/runtime.php`** (источник)
Добавлены 3 новые функции:
- `addResponsiveImages()` — находит все `<img>` теги и обрабатывает их
- `buildSrcsetFromVariants()` — проверяет наличие вариантов изображений (-600, -1200, -1920)
- Теперь `loadTinyContent()` вызывает `addResponsiveImages()` перед возвратом HTML

**Результат:** Когда контент загружается через `loadTinyContent()`, все изображения автоматически получают `srcset` атрибут.

#### 2. **`themes/theme1/auto-alt.php`** (источник)
Улучшена функция `generateSrcset()`:
- Добавлена проверка через `realpath()`
- Приоритет проверок: `__DIR__/` (most reliable) → `$_SERVER['DOCUMENT_ROOT']` → `realpath()`
- Лучше работает с разными конфигурациями сервера

#### 3. **`build/billionairespin.eu/` все файлы** (автогенерируемо)
При сборке эти изменения копируются в build директорию.

## 📸 Как это работает теперь

### До исправления
```html
<img src="img/img.jpg" width="100%">
```
❌ Нет srcset, браузер не знает о вариантах

### После исправления
```html
<img src="img/img.jpg" width="100%"
     srcset="img/img-600.jpg 600w, img/img-1200.jpg 1200w, img/img-1920.jpg 1920w"
     sizes="(max-width: 600px) 600px, (max-width: 1200px) 1200px, 1920px">
```
✅ Браузер выбирает оптимальный размер:
- На мобиле (< 600px) → загружает `img-600.jpg`
- На планшете (< 1200px) → загружает `img-1200.jpg`
- На ПК (> 1200px) → загружает `img-1920.jpg`

## 🎯 Результаты

✅ **Адаптивные изображения работают**
✅ **srcset добавляется автоматически**
✅ **Браузер выбирает оптимальный размер**
✅ **Мобильные пользователи экономят трафик**
✅ **Сайт загружается быстрее**

## 🧪 Тестирование

Проверка в браузере (F12 → Elements):
```html
<img src="img/img.jpg" width="100%"
     srcset="img/img-600.jpg 600w, img/img-1200.jpg 1200w, img/img-1920.jpg 1920w"
     sizes="(max-width: 600px) 600px, (max-width: 1200px) 1200px, 1920px">
```

## 📝 Требования для работы

1. **Оригинальное изображение:** `img/img.jpg`
2. **Варианты:** Обязательно создать файлы:
   - `img/img-600.jpg` (макс. ширина 600px)
   - `img/img-1200.jpg` (макс. ширина 1200px)
   - `img/img-1920.jpg` (макс. ширина 1920px)

Если вариантов нет → srcset не добавляется (браузер просто использует оригинал).

## 🔄 Как использовать

1. Создайте варианты изображения в нужных размерах
2. Сохраните в папку с оригиналом с суффиксами `-600`, `-1200`, `-1920`
3. Добавьте изображение в TinyMCE редактор
4. **Всё!** srcset добавится автоматически

Больше ничего не нужно делать!

## 🚀 Применение

Изменения уже внедрены в проект. При следующей сборке:
```bash
php generator/cli.php --clean
```

Все файлы будут обновлены, и адаптивные изображения будут работать везде.

---

**Дата исправления:** 4 декабря 2025
**Статус:** ✅ Активировано
