# PHP Site Generator

Статический генератор сайтов с поддержкой шаблонов, динамических данных, плагинов, шорткодов и встроенной админ-панелью TinyMCE для редактирования контента. Генератор создаёт готовые сайты без PHP-зависимостей в рантайме — все данные подставляются на этапе сборки.

## Возможности
- ✅ Статическая подстановка данных из конфигурации в любые файлы (PHP, HTML, CSS, JS)
- ✅ Система плагинов с шорткодами (по аналогии с WordPress)
- ✅ **Админ-панель TinyMCE для редактирования контента на продакшене**
- ✅ Динамическая генерация CSS-стилей из конфигурации
- ✅ Чистая сборка без PHP-кода в финальных файлах
- ✅ Автоматическое копирование тем и плагинов
- ✅ Минимум зависимостей, максимум простоты

## Структура проекта
```
public/
├── build/                    # Готовые сайты (генерируются автоматически)
│   └── <domain>/
│       ├── plugins/
│       │   └── tinymce/
│       │       ├── tinymce/          # Self-hosted TinyMCE (без API-ключей)
│       │       │   ├── tinymce.min.js
│       │       │   ├── skins/
│       │       │   ├── themes/
│       │       │   ├── plugins/
│       │       │   └── icons/
│       │       ├── admin/
│       │       │   ├── index.php     # Админ-панель TinyMCE
│       │       │   ├── admin.key     # Хеш пароля (bcrypt)
│       │       │   └── .htaccess     # Защита admin.key
│       │       └── data/
│       │           └── *.html        # Редактируемый контент
├── themes/                   # Шаблоны сайтов
│   └── theme1/
│       ├── header.php
│       ├── footer.php
│       ├── index.php
│       ├── .htaccess         # Роутинг для /tinymce-panel
│       ├── styles/
│       └── img/
├── plugins/                  # Плагины с шорткодами
│   ├── greeting/
│   │   ├── plugin.php        # Логика плагина
│   │   └── style.css         # Стили плагина
│   └── tinymce/
│       ├── tinymce/          # Self-hosted TinyMCE (копируется в build)
│       ├── plugin.php        # Регистрация функции loadTinyContent()
│       ├── runtime.php       # PHP-функция для загрузки контента
│       ├── admin/            # Админ-панель (копируется в build)
│       └── data/             # Хранилище контента
└── generator/
    ├── cli.php              # CLI-интерфейс
    ├── config.php           # Конфигурация проектов
    ├── generate.php         # Логика генерации
    └── shortcodes.php       # Система шорткодов
```

## Требования
- PHP 7.4+ CLI (проверка: `php -v`)
- Windows/OSPanel или любая другая среда с PHP
- Apache с mod_rewrite для роутинга админ-панели
- **TinyMCE self-hosted версия** (уже включена в плагин, API-ключи не нужны)

## Быстрый старт

### Обычная сборка
```powershell
php .\generator\cli.php
```

### Чистая пересборка
```powershell
php .\generator\cli.php --clean
```

Флаг `--clean` полностью удаляет `build/` перед генерацией — используйте для получения чистого результата.

**Важно:** При первой сборке автоматически создаётся `admin.key` с паролем по умолчанию: **admin**

## Конфигурация проекта

Файл `generator/config.php` содержит массив проектов. Пример:

```php
$projects = [
    [
        'domain' => 'site1.local',
        'theme' => './themes/theme1',

        'data' => [
            'title' => 'Мой сайт',
            'description' => 'Описание сайта',
            'h1' => 'Главный заголовок',
        ],

        'postprocess' => ['*.php', '*.html', '*.css'],

        'styles' => [
            ['class' => 'main', 'background' => '#110b66ff'],
            ['class' => 'btn', 'color' => '#ffffff'],
        ],
    ],
];
```

### Поля конфигурации

- **`domain`** — имя папки в `build/`
- **`theme`** — путь к шаблону (копируется в сборку)
- **`data`** — данные для подстановки в шаблоны
- **`postprocess`** — маски файлов для обработки (если пусто — все текстовые)
- **`styles`** — динамические CSS-правила

## Работа с данными

### В шаблонах (theme)
Используйте PHP-плейсхолдеры для статической замены при сборке:

```php
<h1><?php echo $data['h1']?></h1>
<title><?= $data['title'] ?></title>
```

Или используйте функцию `loadTinyContent()` для динамического контента с TinyMCE:

```php
<?php echo loadTinyContent('homepage'); ?>
```

### В сборке (build)
Генератор заменяет плейсхолдеры на статические значения:

```html
<h1>Главный заголовок</h1>
<title>Мой сайт</title>
```

А `loadTinyContent()` загружает HTML из файла в рантайме.

## Админ-панель TinyMCE

### Доступ к админке
После сборки перейдите на:
```
http://<domain>/tinymce-panel
```

**Пароль по умолчанию:** `admin`

### Функционал
- 🔐 Вход через пароль (хранится в `admin.key` как bcrypt-хеш)
- 📝 Список всех редактируемых страниц (файлы из `data/`)
- ✏️ TinyMCE-редактор для WYSIWYG-редактирования (self-hosted, без API-ключей)
- 💾 Сохранение изменений напрямую в HTML-файлы
- 🚪 Кнопка выхода из админки
- 🌐 Полностью автономная работа без внешних CDN

### Использование в шаблонах

**1. Подключите runtime-функции в начале шаблона:**
```php
<?php
if (file_exists(__DIR__ . '/plugins/tinymce/runtime.php')) {
    require_once __DIR__ . '/plugins/tinymce/runtime.php';
}
?>
```

**2. Вставьте контент через функцию:**
```php
<?php echo loadTinyContent('homepage'); ?>
<?php echo loadTinyContent('about'); ?>
```

**3. Создайте соответствующие файлы:**
```
plugins/tinymce/data/homepage.html
plugins/tinymce/data/about.html
```

### Добавление новых страниц
Просто создайте новый HTML-файл в `plugins/tinymce/data/`:
```powershell
echo "<p>New page content</p>" > plugins\tinymce\data\contacts.html
```

Он автоматически появится в админке.

### Смена пароля
Сгенерируйте новый хеш:
```php
php -r "echo password_hash('новый_пароль', PASSWORD_DEFAULT);"
```

Замените содержимое `build/<domain>/plugins/tinymce/admin/admin.key` на полученный хеш.

### Безопасность
- ✅ Файл `admin.key` защищён через `.htaccess`
- ✅ Пароль хранится как bcrypt-хеш
- ✅ Сессии для авторизации
- ✅ Базовая защита от несанкционированного доступа
- ✅ TinyMCE работает локально без отправки данных на сторонние серверы

**Рекомендация:** После первой сборки обязательно смените пароль по умолчанию!

### Обновление TinyMCE
Для обновления TinyMCE до новой версии:
1. Скачайте self-hosted версию с https://www.tiny.cloud/get-tiny/self-hosted/
2. Распакуйте и замените содержимое `plugins/tinymce/tinymce/`
3. Запустите пересборку: `php .\generator\cli.php --clean`

### Обновление проекта через GitHub
Начиная с версии с поддержкой GitHub, вы можете обновлять весь проект прямо из админ-панели:

**Настройка:**
1. Перейдите во вкладку **GitHub Settings** в админке
2. Укажите URL вашего репозитория (например: `https://github.com/semtio/Phorge`)
3. Выберите ветку для обновлений (по умолчанию: `main`)
4. Нажмите **Save Settings**

**Использование:**
1. После настройки появится кнопка **Update from GitHub** в шапке админки
2. При нажатии система выполнит:
   - `git pull origin <branch>` — скачает последние изменения
   - `php generator/cli.php --clean` — пересоберёт все сайты
3. Вы увидите сообщение об успехе или ошибке

**Что обновляется:**
- ✅ Темы (`themes/`)
- ✅ Плагины (`plugins/`)
- ✅ Генератор (`generator/`)
- ✅ Стили и скрипты
- ✅ Конфигурация проектов

**Безопасность:**
- Настройки хранятся в `github.config` (защищён `.htaccess`)
- Только для публичных репозиториев
- Требуется авторизация в админ-панели

**Важно:** Убедитесь, что на сервере установлен Git и есть права на выполнение `git pull`.

## Работа с данными

### В шаблонах (theme)
Используйте PHP-плейсхолдеры для статической замены при сборке:

```php
<h1><?php echo $data['h1']?></h1>
<title><?= $data['title'] ?></title>
```

Или используйте функцию `loadTinyContent()` для динамического контента с TinyMCE:

```php
<?php echo loadTinyContent('homepage'); ?>
```

### В сборке (build)
Генератор заменяет плейсхолдеры на статические значения:

```html
<h1>Главный заголовок</h1>
<title>Мой сайт</title>
```

## Система плагинов

### Структура плагина
```
plugins/my-plugin/
├── plugin.php    # Логика + регистрация шорткодов
└── style.css     # Опциональные стили
```

### Создание плагина

**`plugins/my-plugin/plugin.php`:**
```php
<?php
function my_shortcode($attrs) {
    $text = $attrs['text'] ?? 'Hello';
    return "<div class='my-widget'>{$text}</div>";
}

return [
    'shortcodes' => [
        'myshort' => 'my_shortcode',
    ]
];
```

### Использование шорткодов

В шаблоне:
```php
[myshort text="Привет, мир!"]
[greeting name="Иван" style="casual"]
```

В сборке (результат):
```html
<div class='my-widget'>Привет, мир!</div>
<div class='greeting casual'><p>Привет, Иван! 👋</p></div>
```

### Встроенные плагины

**Greeting** — приветствие с разными стилями:
```php
[greeting name="Имя" style="casual|formal|default"]
```

## Динамические стили

Генератор автоматически создаёт CSS-правила из конфигурации:

```php
'styles' => [
    ['class' => 'header', 'background' => '#000', 'color' => '#fff'],
    ['class' => 'btn', 'background' => 'blue', 'file' => 'styles/custom.css'],
]
```

Результат в `styles/custom.css`:
```css
.header { background-color: #000; color: #fff; }
.btn { background-color: blue; }
```

## Принципы работы

### Процесс генерации
1. **Копирование темы** — файлы из `themes/` → `build/<domain>/`
2. **Генерация admin.key** — создаётся `plugins/tinymce/admin/admin.key` с паролем `admin` (если не существует)
3. **Генерация CSS** — динамические стили добавляются в указанные файлы
4. **Замена данных** — плейсхолдеры `<?php echo $data['key']?>` → статические значения
5. **Обработка шорткодов** — `[shortcode]` → HTML от плагинов
6. **Копирование плагинов** — `plugins/` → `build/<domain>/plugins/`

### Два типа контента
- **Статический (build-time):** `<?php echo $data['key']?>` — заменяется при сборке
- **Динамический (runtime):** `<?php echo loadTinyContent('page')?>` — загружается при открытии страницы

### Работа TinyMCE-плагина
1. Генератор копирует `plugins/tinymce/` в `build/<domain>/plugins/tinymce/`
2. При первом запуске создаётся `admin.key` с паролем по умолчанию
3. Админка доступна по маршруту `/tinymce-panel` (настраивается через `.htaccess`)
4. Контент сохраняется в `data/*.html` и загружается через `loadTinyContent()`
5. Изменения видны сразу без пересборки

## Расширенные возможности

### Маски обработки файлов
```php
'postprocess' => [
    '*.php',              // Все PHP-файлы
    '*.html',             // Все HTML
    'templates/*.php',    // PHP в папке templates
    'index.php',          // Конкретный файл
]
```

### Несколько проектов
Генератор обрабатывает все проекты из массива `$projects` за один запуск:

```php
$projects = [
    ['domain' => 'site1.local', ...],
    ['domain' => 'site2.local', ...],
    ['domain' => 'site3.local', ...],
];
```

## CLI аргументы

- `--clean` — полная очистка `build/` перед сборкой

## Рабочий процесс

1. Настройте проект в `generator/config.php`
2. Создайте/отредактируйте шаблон в `themes/`
3. Используйте плейсхолдеры и шорткоды в шаблоне
4. Запустите генерацию: `php .\generator\cli.php --clean`
5. Готовый сайт в `build/<domain>/`

## Что дальше?

- Создавайте собственные плагины для повторяющегося контента
- Добавляйте новые поля в `data` для гибкости
- Расширяйте систему шорткодов под свои задачи
- Генерируйте 20-30 сайтов в день с минимальными усилиями
- **Используйте TinyMCE для редактирования контента на готовых сайтах**
- Настраивайте стили админки через `identity.md`

## FAQ

**Q: Можно ли редактировать контент после сборки?**
A: Да! Используйте `loadTinyContent()` и TinyMCE-админку. Контент из `data/` редактируется через браузер.

**Q: Как защитить админку?**
A: Файл `admin.key` защищён `.htaccess`. Обязательно смените пароль после первой сборки.

**Q: Нужна ли база данных?**
A: Нет. Весь контент хранится в файлах: статические данные в `config.php`, редактируемый контент в `data/*.html`.

**Q: Можно ли использовать на хостинге?**
A: Да. Сгенерированные сайты работают на любом хостинге с PHP 7.4+ и Apache с mod_rewrite.

**Q: Как добавить новую страницу для редактирования?**
A: Создайте `.html` файл в `plugins/tinymce/data/` и используйте `loadTinyContent('имя_файла')` в шаблоне.

**Q: Разница между `$data['key']` и `loadTinyContent()`?**
A: `$data['key']` — статическая замена при сборке. `loadTinyContent()` — динамическая загрузка из файла при каждом запросе.

**Q: Требуется ли API-ключ для TinyMCE?**
A: Нет! Используется self-hosted версия TinyMCE, которая работает полностью автономно без внешних API и CDN.

**Q: Как обновить TinyMCE до новой версии?**
A: Скачайте актуальную версию с https://www.tiny.cloud/get-tiny/self-hosted/, замените файлы в `plugins/tinymce/tinymce/` и запустите `php .\generator\cli.php --clean`.
