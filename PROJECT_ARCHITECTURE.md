# 📐 Архитектура проекта Forge (Краткое описание)

## 🎯 Суть в двух словах

**Forge** — это генератор, который из трех источников (themes + plugins + config) создает готовые динамические PHP-сайты и помещает их в папку `build/`.

```
themes/ + plugins/ + config.php → [генератор] → build/ready-site/
```

---

## 🗂️ Главное правило

### 🚫 Папка `build/` — ТОЛЬКО ДЛЯ ЧТЕНИЯ

Никогда прямо не редактируем файлы в `build/` — туда попадает **готовый продукт**.

Если что-то нужно изменить:
- Редактируем в `themes/` или `plugins/` или `config.php`
- Запускаем генератор: `php generator/cli.php --clean`
- Результат обновляется в `build/`

---

## 📦 Три источника генерации

### 1️⃣ **`generator/`** — Движок
```
generator/
├── config.php          # Конфиг всех проектов (домены, данные, стили)
├── generate.php        # Основная логика — что и как генерировать
├── cli.php             # Интерфейс запуска (php generator/cli.php)
└── shortcodes.php      # Система [shortcode] обработки
```

**Что здесь:** Настройка проектов, переменные, стили, маршруты генерации.

### 2️⃣ **`themes/`** — Шаблоны
```
themes/
├── theme1/             # Первая тема
│   ├── index.php       # Главная страница
│   ├── header.php      # Заголовок с мета-тегами
│   ├── footer.php      # Подвал
│   ├── auto-alt.php    # Логика адаптивных изображений
│   ├── styles/         # CSS (будут подставлены цвета)
│   ├── scripts/        # JavaScript
│   ├── img/            # Изображения + варианты (-600, -1200, -1920)
│   └── robots.txt      # SEO
└── theme2/             # Вторая тема (как theme1)
```

**Что здесь:** Верстка, структура HTML, базовые стили, изображения.

### 3️⃣ **`plugins/`** — Функциональность
```
plugins/
├── greeting/           # Пример: плагин приветствия
│   ├── plugin.php      # Логика шорткода [greeting]
│   └── style.css       # Стили плагина
└── tinymce/            # Админ-панель для редактирования контента
    ├── plugin.php      # Шорткод [content id="1"]
    ├── runtime.php     # Функция loadTinyContent() для рантайма
    ├── admin/          # Админ-панель (копируется в build)
    ├── data/           # Редактируемый контент (content-1.html, content-2.html)
    └── tinymce/        # Self-hosted редактор (работает без API)
```

**Что здесь:** Расширяемые модули, шорткоды, админ-панель для редактирования.

---

## 🔄 Процесс генерации (пошагово)

**Команда:**
```bash
php generator/cli.php --clean
```

**Что происходит:**

1. **Читает config.php** → список проектов (домены, данные, стили)
2. **Создает папку** `build/billionairespin.eu/`
3. **Копирует тему** `themes/theme1/` в `build/billionairespin.eu/`
4. **Копирует плагины** `plugins/` в `build/billionairespin.eu/plugins/`
5. **Подставляет данные:**
   - `<?php echo $data['title'] ?>` → "My Site"
   - `{{btn_link1}}` → зашифровывает ссылку
6. **Обрабатывает шорткоды:**
   - `[greeting name="John"]` → `<div class="greeting">...</div>`
   - `[content id="1"]` → функция загрузки контента
7. **Генерирует стили** из config
8. **Результат:** готовый сайт в `build/billionairespin.eu/`

---

## 📋 Структура ГОТОВОГО САЙТА в `build/`

```
build/billionairespin.eu/
├── index.php                      # Главная (со всеми подставленными данными)
├── header.php                     # Заголовок + мета-теги + стили
├── footer.php                     # Подвал
├── encrypted.php                  # Автогенерируемый (шифрование ссылок)
├── auto-alt.php                   # Логика адаптивных изображений
├── robots.txt                     # SEO (с подставленным доменом)
├── sitemap.xml                    # Карта сайта
│
├── plugins/
│   ├── greeting/
│   │   ├── plugin.php
│   │   └── style.css
│   └── tinymce/
│       ├── admin/                 # Админ-панель
│       │   ├── index.php
│       │   └── admin.key          # Пароль (bcrypt-хеш)
│       ├── runtime.php            # loadTinyContent()
│       ├── data/                  # Редактируемый контент
│       │   ├── content-1.html
│       │   └── content-2.html
│       └── tinymce/               # Self-hosted редактор
│           ├── tinymce.min.js
│           ├── skins/
│           ├── plugins/
│           └── themes/
│
├── styles/
│   ├── styles.css                 # CSS с подставленными цветами
│   ├── child.css
│   └── tinymce.css
│
├── scripts/
│   └── app.js
│
└── img/                           # Изображения
    ├── img.jpg                    # Оригинал
    ├── img-600.jpg                # Для мобилок
    ├── img-1200.jpg               # Для планшетов
    └── img-1920.jpg               # Для ПК
```

**Важно:** ВСЕ файлы уже готовы к использованию. Никого не пересчитывается, ничего не генерируется на лету. Сайт просто работает.

---

## 🔌 Как это работает на примере

### Пример 1: Подстановка данных

**config.php:**
```php
'data' => [
    'title' => 'My Casino',
    'description' => 'Best online casino'
]
```

**theme1/header.php (до генерации):**
```php
<title><?php echo $data['title']; ?></title>
<meta name="description" content="<?php echo $data['description']; ?>">
```

**build/.../header.php (после генерации):**
```php
<title>My Casino</title>
<meta name="description" content="Best online casino">
```

---

### Пример 2: Шорткоды

**theme1/index.php (до генерации):**
```php
[greeting name="John" style="casual"]
```

**build/.../index.php (после генерации):**
```html
<div class='greeting casual'><p>Привет, John! 👋</p></div>
```

---

### Пример 3: Динамический контент (TinyMCE)

**theme1/index.php (до генерации):**
```php
<?php echo loadTinyContent('homepage'); ?>
```

**build/.../index.php (после генерации):**
```php
<?php
    $__rt = __DIR__ . '/plugins/tinymce/runtime.php';
    if (file_exists($__rt)) { require_once $__rt; }
    echo loadTinyContent('homepage');
?>
```

При открытии страницы браузером `loadTinyContent()` загружает содержимое из `data/homepage.html`.

---

### Пример 4: Динамические стили

**config.php:**
```php
'styles' => [
    ['class' => 'header', 'background' => '#f30c0c', 'file' => 'styles/styles.css'],
    ['class' => 'footer', 'color' => '#ffffff', 'file' => 'styles/styles.css'],
]
```

**styles/styles.css (после генерации):**
```css
.header { background-color: #f30c0c; }
.footer { color: #ffffff; }
```

---

## 🖼️ Адаптивные изображения

**В `themes/theme1/img/` создайте варианты:**
```
img.jpg        (оригинал, например 1920x1080)
img-600.jpg    (для мобилок)
img-1200.jpg   (для планшетов)
img-1920.jpg   (для ПК)
```

**В шаблоне просто:**
```html
<img src="img/img.jpg" alt="My photo">
```

**После генерации `auto-alt.php` автоматически добавит:**
```html
<img src="img/img.jpg" alt="My photo"
     srcset="img/img-600.jpg 600w, img/img-1200.jpg 1200w, img/img-1920.jpg 1920w"
     sizes="(max-width: 600px) 600px, (max-width: 1200px) 1200px, 1920px">
```

Браузер выберет нужный размер в зависимости от устройства.

---

## 🔐 Админ-панель TinyMCE

### Что это такое?

**Админ-панель** — интерфейс для редактирования содержимого сайта БЕЗ ПЕРЕСБОРКИ.

- После сборки админка доступна по адресу: `http://site.com/tinymce-panel`
- Пароль: `admin` (по умолчанию)
- Позволяет редактировать контент в браузере в реальном времени
- Self-hosted TinyMCE (работает без API-ключей и интернета)

### Использование в шаблоне

**theme1/index.php:**
```php
<?php echo loadTinyContent('homepage'); ?>
```

Это загружает содержимое из `plugins/tinymce/data/homepage.html`.

### Редактирование через админку

1. Откройте `http://billionairespin.eu/tinymce-panel`
2. Введите пароль: `admin`
3. Выберите страницу из списка
4. Редактируйте контент в TinyMCE
5. Нажмите "Save"
6. Содержимое сохраняется в `data/homepage.html`

### Добавление новой редактируемой страницы

Создайте файл `plugins/tinymce/data/new-page.html`:
```html
<h2>New Page Title</h2>
<p>Content here...</p>
```

Используйте в шаблоне:
```php
<?php echo loadTinyContent('new-page'); ?>
```

---

## ⚡ Команды

### Обычная сборка (обновление существующих файлов)
```bash
php generator/cli.php
```

### Чистая пересборка (удалить build и создать заново)
```bash
php generator/cli.php --clean
```

---

## 🚀 Рабочий цикл

1. **Редактируем** `generator/config.php` — меняем данные, добавляем новые проекты
2. **Редактируем** `themes/` и `plugins/` — добавляем функциональность, верстку
3. **Запускаем** `php generator/cli.php --clean` — собираем все в `build/`
4. **Проверяем** сайт в браузере
5. **После сборки** редактируем контент через админку TinyMCE (если нужно)
6. **Никогда не редактируем** файлы в `build/` напрямую!

---

## 📊 Три типа контента

| Тип | Когда меняется | Как использовать | Пример |
|-----|----------------|------------------|---------|
| **Статический** | При сборке | `<?php echo $data['title']; ?>` | Заголовок, мета-теги |
| **Из конфига** | При сборке | Поле `'title'` в config | Цвета, ссылки, кнопки |
| **Динамический** | В рантайме (админка) | `<?php echo loadTinyContent('page'); ?>` | Редактируемый текст |

---

## ✅ Контрольный список

- ✅ `generator/config.php` — настроен правильно
- ✅ `themes/theme1/` — существует и содержит базовую верстку
- ✅ `plugins/tinymce/` — находится на месте
- ✅ Команда `php generator/cli.php --clean` выполняется без ошибок
- ✅ Содержимое появляется в `build/billionairespin.eu/`
- ✅ Сайт открывается в браузере
- ✅ Админка доступна по `/tinymce-panel`
- ✅ Контент редактируется через админку

---

## 🔥 Главные выводы

1. **BUILD — только для чтения.** Все редактирования делаем в source-файлах.
2. **Три источника:** config → themes → plugins = готовый сайт
3. **Генератор — автоматический.** Запустили, сидим, ждем результата.
4. **TinyMCE — для конечного пользователя.** Они редактируют контент, не трогая код.
5. **Адаптивные изображения — встроены.** Создал варианты -600/-1200/-1920 и забыл.
6. **Шорткоды — расширяемость.** Легко добавить свой функционал через plugins.

---

## 📚 Где что находится

| Что нужно? | Где искать? |
|-----------|-----------|
| Добавить данные в шаблон | `generator/config.php` → поле `data` |
| Изменить верстку | `themes/theme1/index.php` |
| Изменить CSS | `themes/theme1/styles/styles.css` |
| Добавить изображение | `themes/theme1/img/` |
| Создать плагин | `plugins/my-plugin/plugin.php` |
| Отредактировать контент | Админка TinyMCE на сайте |
| Понять, что сломалось | Заглянуть в `build/` и сравнить с source |

---

**Автор анализа:** GitHub Copilot (2025-12-04)
**На основе:** Анализ исходного кода проекта Forge
